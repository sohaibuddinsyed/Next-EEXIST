version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        preBuild:
          commands:
            - npx pnpm@9 install --no-frozen-lockfile
        build:
          commands:
            - npx pnpm@9 build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
